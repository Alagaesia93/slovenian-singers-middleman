<% if defined?(singer) %>
  <section id="about">
    <header class="major">
      <a href="<%= url_for("index.html", anchor: 'singers') %>">←</a>
      <h1><%= singer.first_name + " " + singer.last_name %></h1><h4> <%= singer.send("dates_#{I18n.locale}") %> </h4>
    </header>
    <p style="text-align: justify">
      <%= singer.send("biography_#{I18n.locale}") %>
    </p>

    <h5><%= t(:roles) %></h5>
    <ul>
      <%= singer.send("roles_#{I18n.locale}") %>
    </ul>

    <% if singer.names.present? %>
      <h5><%= t(:names) %></h5>
      <p><%= singer.names %></p>
    <% end %>
  </section>

  <!-- Two -->
  <section id="videos">
    <h2><%= t(:recordings) %></h2>
    <div class="row">
      <% singer.recordings.each do |recording| %>
        <article class="col-12 work-item align-center">
          <% if recording.youtube_url.present? %>
            <iframe width="560" height="315" src="<%= recording.youtube_url %>" frameborder="0"
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
              allowfullscreen loading="lazy"
              title="<%= recording.send("title_#{I18n.locale}") %>"></iframe>
          <% else %>
            <audio src="<%= recording.audio_url %>" controls="true"></audio>
          <% end %>
          <h3><%= recording.send("title_#{I18n.locale}") %></h3>
        </article>
      <% end %>
      <% if singer.playlists.present? %>
        <article class="col-12 work-item align-center">
          <iframe width="560" height="315" src="<%= singer.playlist.url %>" frameborder="0"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen loading="lazy"
            title="<%= singer.playlist.send("title_#{I18n.locale}") %>"></iframe>
          <h3><%= singer.playlist.send("title_#{I18n.locale}") %></h3>
          <h4><%= singer.playlist.recording_count + " " + t(:number_of_recordings) %></h4>
        </article>
      <% end %>
    </div>
  </section>

  <section id="pictures">
    <h2><%= t(:pictures) %></h2>
    <div class="row">
      <% singer.pictures.each do |pic| %>
        <div class="col-4">
          <%= image_tag(pic.path, width: '100%') %>
          <h2><%= pic.send("title_#{I18n.locale}") %></h2>
          <h4>
            <a href="<%= pic.source_link %>" target="_blank" rel="noopener"><%= pic.source %></a>
          </h4>
          <h5><%= pic.credit %></h5>
        </div>
      <% end %>
    </div>
  </section>

  <!-- Three -->
  <section id="ref">
    <h2><%= t(:more) %></h2>
    <div class="row">
      <div class="col-8 col-12-small">
        <ul>
          <% singer.links.each do |link| %>
            <li>
              <a href="<%= link.url %>" target="_blank" rel="noopener">
                <%= link.title %>
              </a>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </section>
<% end %>
